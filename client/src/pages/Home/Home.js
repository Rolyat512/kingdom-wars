import React from 'react'
import { Box } from "@mui/material";
import styles from "./styles";
// import { Timer } from "../../components/Timer/time"

const Home = () => {
  return (
    <Box sx={{ ...styles.mainContainer }}>
        <Box sx={{...styles.skillBox}}> </Box>
    </Box>
  )
}

export default Home
